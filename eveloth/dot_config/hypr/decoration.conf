decoration {
    rounding = 4
    rounding_power = 10.0

    # active_opacity = 0.6
    # inactive_opacity = 0.9
    # fullscreen_opacity = 1.0

    # Looks cool but unfortunately that's not what tiling is for
    dim_inactive = false
    
    blur {
        enabled = true
        size = 1
        passes = 6
        new_optimizations = true
        ignore_opacity = true
        xray = true
    }

    shadow {
        enabled = true
        range = 30
        render_power = 100
        color = rgb(222222)
    }
}
